mobsf config setup :
terminal : bash
cd ~/.mobsf

// perintah stop bawaan docker
docker compose ps

// perintah stop bawaan docker
docker compose stop mobsf
docker compose kill mobsf // cara paksa "tidak disarankan"
